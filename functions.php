<?php

/**
 * Return the sum of two numbers
 * @param int $a The first number
 * @param int $b The second number
 * @return int The sum of the two numbeers
 */
function add(int $a, int $b): int
{
    return $a + $b;
}